Feature 01

- Review if Pre-Production Hardening is documented. if not create a markdown specific to this task
- Enable Swagger UI for API documentation and testing
- Implement health check endpoint (`GET /api/v1/health`) that returns 200 OK
- Implement metrics endpoint (`GET /api/v1/metrics`) that returns basic stats (uptime, request count, error count)
- Write unit tests for health check and metrics endpoints
- Write integration tests for health check and metrics endpoints
- Create a comprehensive README.md with setup instructions, load testing, API documentation, and contribution guidelines
- Create another comprehensive documentation, about the design how they are implemented and why. The meaning of dek, kek and all explained in simple terms
- Document the key rotation process in detail, including how to trigger it, expected outcomes, and troubleshooting steps
